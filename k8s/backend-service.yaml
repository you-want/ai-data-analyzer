apiVersion: v1
kind: Service
metadata:
  name: ai-data-analyzer-backend
  labels:
    app: ai-data-analyzer
    component: backend
spec:
  type: ClusterIP
  selector:
    app: ai-data-analyzer
    component: backend
  ports:
    - name: http
      port: 3001
      targetPort: 3001
