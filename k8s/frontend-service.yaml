apiVersion: v1
kind: Service
metadata:
  name: ai-data-analyzer-frontend
  labels:
    app: ai-data-analyzer
    component: frontend
spec:
  type: ClusterIP
  selector:
    app: ai-data-analyzer
    component: frontend
  ports:
    - name: http
      port: 3000
      targetPort: 3000
